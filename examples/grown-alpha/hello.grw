target "gros.x86.bios.longmode.grogan.v1"

fn main() -> void {
    let total: i32 = 0;
    let i: i32 = 1;
    while (i < 8) {
        total = total + i;
        i = i + 1;
    }
    print_i32(total);
}
